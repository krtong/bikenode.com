---
layout: dashboard.njk
title: Following & Followers
subtitle: Manage your cycling connections
---

<style>
  .following-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 32px;
  }

  .following-stats {
    display: flex;
    gap: 32px;
  }

  .following-stat {
    text-align: center;
  }

  .stat-count {
    font-size: 32px;
    font-weight: bold;
    color: var(--accent);
  }

  .stat-label {
    font-size: 14px;
    color: var(--text-secondary);
    margin-top: 4px;
  }

  .tabs-container {
    display: flex;
    gap: 24px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 32px;
  }

  .tab-btn {
    padding: 12px 0;
    background: none;
    border: none;
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    font-size: 16px;
    font-weight: 500;
  }

  .tab-btn:hover {
    color: var(--text-primary);
  }

  .tab-btn.active {
    color: var(--accent);
  }

  .tab-btn.active::after {
    content: '';
    position: absolute;
    bottom: -1px;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--accent);
  }

  .search-bar {
    background: var(--card-bg);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 12px 16px;
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 24px;
  }

  .search-bar input {
    background: none;
    border: none;
    outline: none;
    flex: 1;
    color: var(--text-primary);
  }

  .users-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 20px;
  }

  .user-card {
    background: var(--card-bg);
    border-radius: 12px;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    transition: all 0.2s ease;
  }

  .user-card:hover {
    transform: translateY(-2px);
    background: rgba(255, 255, 255, 0.05);
  }

  .user-avatar {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 32px;
    margin-bottom: 16px;
  }

  .user-name {
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .user-username {
    font-size: 14px;
    color: var(--text-secondary);
    margin-bottom: 8px;
  }

  .user-bio {
    font-size: 14px;
    color: var(--text-secondary);
    margin-bottom: 16px;
    line-height: 1.5;
  }

  .user-stats {
    display: flex;
    gap: 24px;
    margin-bottom: 16px;
  }

  .user-stat {
    text-align: center;
  }

  .user-stat-value {
    font-weight: 600;
    color: var(--accent);
  }

  .user-stat-label {
    font-size: 12px;
    color: var(--text-secondary);
  }

  .user-actions {
    display: flex;
    gap: 12px;
    width: 100%;
  }

  .follow-btn {
    flex: 1;
    background: var(--accent);
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .follow-btn:hover {
    background: var(--accent-hover);
  }

  .follow-btn.following {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .message-btn {
    background: none;
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: var(--text-primary);
    padding: 10px;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .message-btn:hover {
    background: rgba(255, 255, 255, 0.05);
  }

  .mutual-badge {
    background: rgba(88, 101, 242, 0.1);
    color: var(--accent);
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 12px;
    margin-bottom: 12px;
  }

  .suggested-section {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
    border-radius: 12px;
    padding: 32px;
    margin-bottom: 32px;
    text-align: center;
  }

  .suggested-title {
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 12px;
  }

  .suggested-subtitle {
    font-size: 16px;
    opacity: 0.9;
    margin-bottom: 24px;
  }

  .empty-state {
    text-align: center;
    padding: 60px 20px;
  }

  .empty-icon {
    font-size: 64px;
    margin-bottom: 16px;
    opacity: 0.5;
  }

  .empty-title {
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .empty-text {
    color: var(--text-secondary);
    margin-bottom: 24px;
  }

  .load-more {
    text-align: center;
    margin-top: 32px;
  }

  @media (max-width: 768px) {
    .following-header {
      flex-direction: column;
      gap: 16px;
    }

    .users-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<div class="following-header">
  <div>
    <h1>Following & Followers</h1>
    <p style="color: var(--text-secondary); margin-top: 8px;">Connect with fellow cyclists</p>
  </div>
  
  <div class="following-stats">
    <div class="following-stat">
      <div class="stat-count">248</div>
      <div class="stat-label">Following</div>
    </div>
    <div class="following-stat">
      <div class="stat-count">1,234</div>
      <div class="stat-label">Followers</div>
    </div>
  </div>
</div>

<div class="tabs-container">
  <button class="tab-btn active" data-tab="followers">Followers</button>
  <button class="tab-btn" data-tab="following">Following</button>
  <button class="tab-btn" data-tab="suggested">Suggested</button>
  <button class="tab-btn" data-tab="requests">Requests (3)</button>
</div>

<div class="search-bar">
  <span>🔍</span>
  <input type="text" placeholder="Search users...">
</div>

<!-- Followers Tab -->
<div class="tab-content" id="followers-tab">
  <div class="users-grid">
    <div class="user-card">
      <div class="user-avatar">SC</div>
      <h3 class="user-name">Sarah Chen</h3>
      <p class="user-username">@sarahrides</p>
      <p class="user-bio">Century rider and coffee enthusiast ☕ Always chasing sunrise rides</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">2.5k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">142</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="mutual-badge">Follows you back</div>
      
      <div class="user-actions">
        <button class="follow-btn following">Following</button>
        <button class="message-btn">💬</button>
      </div>
    </div>

    <div class="user-card">
      <div class="user-avatar">MJ</div>
      <h3 class="user-name">Mike Johnson</h3>
      <p class="user-username">@mikemtb</p>
      <p class="user-bio">Mountain biking is life 🏔️ Trail builder and adventure seeker</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">1.8k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">89</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="user-actions">
        <button class="follow-btn">Follow Back</button>
        <button class="message-btn">💬</button>
      </div>
    </div>

    <div class="user-card">
      <div class="user-avatar">EW</div>
      <h3 class="user-name">Emma Wilson</h3>
      <p class="user-username">@emmawheels</p>
      <p class="user-bio">Gravel explorer and bikepacking enthusiast. Currently planning a coast-to-coast adventure</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">3.2k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">178</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="mutual-badge">Follows you back</div>
      
      <div class="user-actions">
        <button class="follow-btn following">Following</button>
        <button class="message-btn">💬</button>
      </div>
    </div>

    <div class="user-card">
      <div class="user-avatar">AR</div>
      <h3 class="user-name">Alex Rodriguez</h3>
      <p class="user-username">@alexraces</p>
      <p class="user-bio">Cat 2 racer | Team Velocity | Always training, never stopping</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">5.1k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">234</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="user-actions">
        <button class="follow-btn">Follow Back</button>
        <button class="message-btn">💬</button>
      </div>
    </div>

    <div class="user-card">
      <div class="user-avatar">LP</div>
      <h3 class="user-name">Lisa Park</h3>
      <p class="user-username">@lisaonbikes</p>
      <p class="user-bio">Weekend warrior and group ride organizer. Join our Sunday rides!</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">1.2k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">67</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="mutual-badge">12 mutual friends</div>
      
      <div class="user-actions">
        <button class="follow-btn">Follow Back</button>
        <button class="message-btn">💬</button>
      </div>
    </div>

    <div class="user-card">
      <div class="user-avatar">TD</div>
      <h3 class="user-name">Tom Davies</h3>
      <p class="user-username">@tomdavies</p>
      <p class="user-bio">Commuter cyclist turned endurance rider. Training for my first double century</p>
      
      <div class="user-stats">
        <div class="user-stat">
          <div class="user-stat-value">2.8k</div>
          <div class="user-stat-label">miles/year</div>
        </div>
        <div class="user-stat">
          <div class="user-stat-value">156</div>
          <div class="user-stat-label">rides</div>
        </div>
      </div>
      
      <div class="user-actions">
        <button class="follow-btn">Follow Back</button>
        <button class="message-btn">💬</button>
      </div>
    </div>
  </div>

  <div class="load-more">
    <button class="btn-secondary">Load More</button>
  </div>
</div>

<!-- Following Tab (Hidden) -->
<div class="tab-content" id="following-tab" style="display: none;">
  <div class="users-grid">
    <!-- Similar user cards for people you follow -->
  </div>
</div>

<!-- Suggested Tab (Hidden) -->
<div class="tab-content" id="suggested-tab" style="display: none;">
  <div class="suggested-section">
    <h2 class="suggested-title">Discover New Riders</h2>
    <p class="suggested-subtitle">Based on your riding style and mutual connections</p>
    <button class="btn-primary btn-large">Find Riders Near Me</button>
  </div>
  
  <div class="users-grid">
    <!-- Suggested users -->
  </div>
</div>

<!-- Requests Tab (Hidden) -->
<div class="tab-content" id="requests-tab" style="display: none;">
  <div class="users-grid">
    <!-- Follow requests if privacy settings require approval -->
  </div>
</div>

<script>
  // Tab switching
  document.querySelectorAll('.tab-btn').forEach(tab => {
    tab.addEventListener('click', () => {
      // Update active tab
      document.querySelectorAll('.tab-btn').forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      
      // Show corresponding content
      const tabName = tab.dataset.tab;
      document.querySelectorAll('.tab-content').forEach(content => {
        content.style.display = 'none';
      });
      
      // For demo, only followers tab has content
      if (tabName === 'followers') {
        document.getElementById('followers-tab').style.display = 'block';
      }
    });
  });

  // Follow/unfollow buttons
  document.querySelectorAll('.follow-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      if (btn.classList.contains('following')) {
        btn.classList.remove('following');
        btn.textContent = 'Follow';
      } else {
        btn.classList.add('following');
        btn.textContent = 'Following';
      }
    });
  });

  // Message buttons
  document.querySelectorAll('.message-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      window.location.href = '/messages/';
    });
  });

  // Search functionality
  const searchInput = document.querySelector('.search-bar input');
  searchInput.addEventListener('input', (e) => {
    const searchTerm = e.target.value.toLowerCase();
    document.querySelectorAll('.user-card').forEach(card => {
      const name = card.querySelector('.user-name').textContent.toLowerCase();
      const username = card.querySelector('.user-username').textContent.toLowerCase();
      const bio = card.querySelector('.user-bio').textContent.toLowerCase();
      
      if (name.includes(searchTerm) || username.includes(searchTerm) || bio.includes(searchTerm)) {
        card.style.display = 'flex';
      } else {
        card.style.display = 'none';
      }
    });
  });
</script>