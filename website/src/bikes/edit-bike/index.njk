---
layout: bikenode-main-layout-01/bikenode-main-layout-01
title: Edit Bike
subtitle: Update your bike details and specifications
showContentHeader: false
breadcrumb:
  - title: Virtual Garage
    url: /virtual-garage-dashboard/
  - title: Edit Bike
---

<div class="edit-bike-container">
    <!-- Header -->
    <div class="edit-header">
        <div class="header-left">
            <h1>Edit Bike Details</h1>
            <p class="edit-subtitle">Update your bike information, photos, and specifications</p>
        </div>
        <div class="header-actions">
            <button class="btn-secondary" onclick="cancelEdit()">Cancel</button>
            <button class="btn-primary" onclick="saveBike()">
                <span class="icon">💾</span>
                Save Changes
            </button>
        </div>
    </div>

    <!-- Edit Form -->
    <div class="edit-content">
        <div class="edit-sections">
            <!-- Basic Information -->
            <section class="edit-section">
                <h2 class="section-title">Basic Information</h2>
                <div class="form-grid">
                    <div class="form-group">
                        <label for="bike-name">Bike Name</label>
                        <input type="text" id="bike-name" value="2007 Aprilia Tuono 1000R" placeholder="Enter bike name">
                    </div>
                    
                    <div class="form-group">
                        <label for="bike-year">Year</label>
                        <input type="number" id="bike-year" value="2007" min="1900" max="2025">
                    </div>
                    
                    <div class="form-group">
                        <label for="bike-make">Make</label>
                        <input type="text" id="bike-make" value="Aprilia" placeholder="Manufacturer">
                    </div>
                    
                    <div class="form-group">
                        <label for="bike-model">Model</label>
                        <input type="text" id="bike-model" value="Tuono 1000R" placeholder="Model name">
                    </div>
                    
                    <div class="form-group">
                        <label for="bike-type">Type</label>
                        <select id="bike-type">
                            <option value="naked-sport" selected>Naked Sport</option>
                            <option value="sport-bike">Sport Bike</option>
                            <option value="cruiser">Cruiser</option>
                            <option value="touring">Touring</option>
                            <option value="adventure">Adventure</option>
                            <option value="dirt-bike">Dirt Bike</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="bike-category">Category</label>
                        <select id="bike-category">
                            <option value="motorcycle" selected>Motorcycle</option>
                            <option value="bicycle">Bicycle</option>
                            <option value="ebike">E-Bike</option>
                        </select>
                    </div>
                </div>
            </section>

            <!-- Photos Section -->
            <section class="edit-section">
                <h2 class="section-title">Photos</h2>
                <div class="photos-manager">
                    <div class="photos-grid">
                        <div class="photo-item">
                            <img src="/assets/images/kevintong/tuono/BAA64891-1650-46D4-BBD7-E80A7C1FE385_1_105_c.jpeg" alt="Bike photo">
                            <div class="photo-actions">
                                <button class="photo-action" title="Set as primary">⭐</button>
                                <button class="photo-action" title="Delete photo">🗑️</button>
                            </div>
                        </div>
                        
                        <div class="photo-item">
                            <img src="/assets/images/kevintong/tuono/4876820D-6A68-47C5-B12E-1E0AEA8F0F14_1_105_c.jpeg" alt="Bike photo">
                            <div class="photo-actions">
                                <button class="photo-action" title="Set as primary">⭐</button>
                                <button class="photo-action" title="Delete photo">🗑️</button>
                            </div>
                        </div>
                        
                        <div class="photo-upload">
                            <input type="file" id="photo-upload" multiple accept="image/*" style="display: none;">
                            <label for="photo-upload" class="upload-area">
                                <div class="upload-icon">📷</div>
                                <div class="upload-text">Add Photos</div>
                            </label>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Specifications -->
            <section class="edit-section">
                <h2 class="section-title">Specifications</h2>
                <div class="specs-tabs">
                    <button class="spec-tab active" data-tab="engine">Engine</button>
                    <button class="spec-tab" data-tab="performance">Performance</button>
                    <button class="spec-tab" data-tab="dimensions">Dimensions</button>
                    <button class="spec-tab" data-tab="other">Other</button>
                </div>
                
                <div class="spec-content">
                    <!-- Engine Tab -->
                    <div class="spec-tab-content active" id="engine">
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="engine-type">Engine Type</label>
                                <input type="text" id="engine-type" value="4-stroke, liquid-cooled V4">
                            </div>
                            <div class="form-group">
                                <label for="displacement">Displacement</label>
                                <input type="text" id="displacement" value="998cc">
                            </div>
                            <div class="form-group">
                                <label for="bore">Bore</label>
                                <input type="text" id="bore" value="60mm">
                            </div>
                            <div class="form-group">
                                <label for="stroke">Stroke</label>
                                <input type="text" id="stroke" value="44.3mm">
                            </div>
                            <div class="form-group">
                                <label for="compression">Compression Ratio</label>
                                <input type="text" id="compression" value="11.5:1">
                            </div>
                            <div class="form-group">
                                <label for="fuel-system">Fuel System</label>
                                <input type="text" id="fuel-system" value="Electronic fuel injection">
                            </div>
                        </div>
                    </div>
                    
                    <!-- Performance Tab -->
                    <div class="spec-tab-content" id="performance">
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="power">Max Power</label>
                                <input type="text" id="power" value="139 hp @ 10,000 rpm">
                            </div>
                            <div class="form-group">
                                <label for="torque">Max Torque</label>
                                <input type="text" id="torque" value="77 lb-ft @ 8,000 rpm">
                            </div>
                            <div class="form-group">
                                <label for="top-speed">Top Speed</label>
                                <input type="text" id="top-speed" value="160 mph">
                            </div>
                            <div class="form-group">
                                <label for="acceleration">0-60 mph</label>
                                <input type="text" id="acceleration" value="3.2 seconds">
                            </div>
                        </div>
                    </div>
                    
                    <!-- Dimensions Tab -->
                    <div class="spec-tab-content" id="dimensions">
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="length">Length</label>
                                <input type="text" id="length" value="82.3 in">
                            </div>
                            <div class="form-group">
                                <label for="width">Width</label>
                                <input type="text" id="width" value="32.5 in">
                            </div>
                            <div class="form-group">
                                <label for="height">Height</label>
                                <input type="text" id="height" value="46.1 in">
                            </div>
                            <div class="form-group">
                                <label for="wheelbase">Wheelbase</label>
                                <input type="text" id="wheelbase" value="56.3 in">
                            </div>
                            <div class="form-group">
                                <label for="seat-height">Seat Height</label>
                                <input type="text" id="seat-height" value="32.1 in">
                            </div>
                            <div class="form-group">
                                <label for="dry-weight">Dry Weight</label>
                                <input type="text" id="dry-weight" value="374 lbs">
                            </div>
                        </div>
                    </div>
                    
                    <!-- Other Tab -->
                    <div class="spec-tab-content" id="other">
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="fuel-capacity">Fuel Capacity</label>
                                <input type="text" id="fuel-capacity" value="4.5 gallons">
                            </div>
                            <div class="form-group">
                                <label for="mileage">Current Mileage</label>
                                <input type="number" id="mileage" value="18200">
                            </div>
                            <div class="form-group">
                                <label for="color">Color</label>
                                <input type="text" id="color" value="Red">
                            </div>
                            <div class="form-group">
                                <label for="vin">VIN</label>
                                <input type="text" id="vin" placeholder="Vehicle Identification Number">
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Personal Notes -->
            <section class="edit-section">
                <h2 class="section-title">Personal Notes</h2>
                <div class="form-group">
                    <label for="bike-notes">Notes</label>
                    <textarea id="bike-notes" rows="6" placeholder="Add your personal notes about this bike...">My main track day weapon! This Tuono is the perfect balance of street comfort and track capability. The V4 engine is absolutely intoxicating - nothing sounds better on the track. I've done suspension work with Öhlins components and it handles like a dream at Thunderhill and Laguna Seca.</textarea>
                </div>
            </section>

            <!-- Privacy Settings -->
            <section class="edit-section">
                <h2 class="section-title">Privacy Settings</h2>
                <div class="privacy-options">
                    <div class="privacy-option">
                        <input type="radio" id="privacy-public" name="privacy" value="public" checked>
                        <label for="privacy-public">
                            <strong>Public</strong>
                            <span>Visible to everyone</span>
                        </label>
                    </div>
                    <div class="privacy-option">
                        <input type="radio" id="privacy-community" name="privacy" value="community">
                        <label for="privacy-community">
                            <strong>Community Only</strong>
                            <span>Visible to your communities</span>
                        </label>
                    </div>
                    <div class="privacy-option">
                        <input type="radio" id="privacy-private" name="privacy" value="private">
                        <label for="privacy-private">
                            <strong>Private</strong>
                            <span>Only visible to you</span>
                        </label>
                    </div>
                </div>
            </section>
        </div>
    </div>
</div>

<style>
.edit-bike-container {
    max-width: 1000px;
    margin: 0 auto;
}

.edit-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 32px;
    gap: 24px;
}

.header-left h1 {
    color: var(--text-primary);
    margin-bottom: 8px;
    font-size: 1.8rem;
}

.edit-subtitle {
    color: var(--text-secondary);
    margin: 0;
}

.header-actions {
    display: flex;
    gap: 12px;
    flex-shrink: 0;
}

.edit-content {
    display: flex;
    flex-direction: column;
    gap: 32px;
}

.edit-section {
    background: var(--card-bg);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 32px;
}

.section-title {
    color: var(--text-primary);
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 24px;
}

.form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.form-group label {
    color: var(--text-primary);
    font-weight: 600;
    font-size: 0.9rem;
}

.form-group input,
.form-group select,
.form-group textarea {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: var(--text-primary);
    padding: 12px 16px;
    border-radius: 8px;
    font-size: 14px;
    transition: all 0.2s ease;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: var(--accent);
    background: rgba(255, 255, 255, 0.08);
}

.form-group textarea {
    resize: vertical;
    font-family: inherit;
    line-height: 1.5;
}

.photos-manager {
    margin-top: 16px;
}

.photos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 16px;
}

.photo-item {
    position: relative;
    aspect-ratio: 1;
    border-radius: 12px;
    overflow: hidden;
    background: rgba(255, 255, 255, 0.05);
}

.photo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.photo-actions {
    position: absolute;
    top: 8px;
    right: 8px;
    display: flex;
    gap: 4px;
    opacity: 0;
    transition: opacity 0.2s ease;
}

.photo-item:hover .photo-actions {
    opacity: 1;
}

.photo-action {
    background: rgba(0, 0, 0, 0.7);
    border: none;
    color: white;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    transition: background 0.2s ease;
}

.photo-action:hover {
    background: rgba(0, 0, 0, 0.9);
}

.photo-upload {
    aspect-ratio: 1;
}

.upload-area {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    border: 2px dashed rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.upload-area:hover {
    border-color: var(--accent);
    background: rgba(88, 101, 242, 0.05);
}

.upload-icon {
    font-size: 2rem;
    margin-bottom: 8px;
}

.upload-text {
    color: var(--text-secondary);
    font-weight: 600;
}

.specs-tabs {
    display: flex;
    gap: 8px;
    margin-bottom: 24px;
    flex-wrap: wrap;
}

.spec-tab {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: var(--text-secondary);
    padding: 10px 20px;
    border-radius: 20px;
    cursor: pointer;
    font-size: 14px;
    font-weight: 500;
    transition: all 0.2s ease;
}

.spec-tab:hover {
    border-color: var(--accent);
    color: var(--accent);
}

.spec-tab.active {
    background: var(--accent);
    border-color: var(--accent);
    color: white;
}

.spec-tab-content {
    display: none;
}

.spec-tab-content.active {
    display: block;
}

.privacy-options {
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.privacy-option {
    display: flex;
    align-items: center;
    gap: 12px;
}

.privacy-option input[type="radio"] {
    width: 18px;
    height: 18px;
    accent-color: var(--accent);
}

.privacy-option label {
    display: flex;
    flex-direction: column;
    gap: 4px;
    cursor: pointer;
}

.privacy-option label strong {
    color: var(--text-primary);
    font-size: 0.95rem;
}

.privacy-option label span {
    color: var(--text-secondary);
    font-size: 0.85rem;
}

@media (max-width: 768px) {
    .edit-header {
        flex-direction: column;
        align-items: stretch;
    }
    
    .header-actions {
        justify-content: flex-end;
    }
    
    .form-grid {
        grid-template-columns: 1fr;
    }
    
    .specs-tabs {
        overflow-x: auto;
        scrollbar-width: none;
        -ms-overflow-style: none;
    }
    
    .specs-tabs::-webkit-scrollbar {
        display: none;
    }
}
</style>

<script>
// Spec tabs functionality
document.querySelectorAll('.spec-tab').forEach(tab => {
    tab.addEventListener('click', function() {
        const tabId = this.dataset.tab;
        
        // Remove active class from all tabs and content
        document.querySelectorAll('.spec-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.spec-tab-content').forEach(c => c.classList.remove('active'));
        
        // Add active class to clicked tab and corresponding content
        this.classList.add('active');
        document.getElementById(tabId).classList.add('active');
    });
});

// Photo upload handling
document.getElementById('photo-upload').addEventListener('change', function(e) {
    const files = Array.from(e.target.files);
    console.log('Photos selected:', files.length);
    
    files.forEach(file => {
        if (file.type.startsWith('image/')) {
            console.log('Processing image:', file.name);
            // In a real app, upload the image here
        }
    });
});

// Photo action buttons
document.querySelectorAll('.photo-action').forEach(btn => {
    btn.addEventListener('click', function(e) {
        e.stopPropagation();
        const action = this.title;
        console.log('Photo action:', action);
        
        if (action === 'Delete photo') {
            if (confirm('Are you sure you want to delete this photo?')) {
                this.closest('.photo-item').remove();
            }
        } else if (action === 'Set as primary') {
            // Mark as primary photo
            alert('Photo set as primary!');
        }
    });
});

// Form actions
function saveBike() {
    // Collect all form data
    const bikeData = {
        name: document.getElementById('bike-name').value,
        year: document.getElementById('bike-year').value,
        make: document.getElementById('bike-make').value,
        model: document.getElementById('bike-model').value,
        type: document.getElementById('bike-type').value,
        category: document.getElementById('bike-category').value,
        notes: document.getElementById('bike-notes').value,
        privacy: document.querySelector('input[name="privacy"]:checked').value
        // ... collect all other fields
    };
    
    console.log('Saving bike data:', bikeData);
    alert('Bike updated successfully!');
    window.location.href = '/virtual-garage-dashboard/';
}

function cancelEdit() {
    if (confirm('Are you sure you want to cancel? Any unsaved changes will be lost.')) {
        window.location.href = '/virtual-garage-dashboard/';
    }
}

// Auto-save draft (optional)
let saveTimeout;
document.querySelectorAll('input, select, textarea').forEach(field => {
    field.addEventListener('input', function() {
        clearTimeout(saveTimeout);
        saveTimeout = setTimeout(() => {
            console.log('Auto-saving draft...');
            // Save draft to localStorage or server
        }, 2000);
    });
});
</script>